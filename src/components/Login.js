import React, { useState } from 'react';

const Login = () => {
  return <div>
      <div>Login</div>
      이메일<input type="text" onChange></input>
      암&nbsp;호<input type="password" onChange></input>
      <button onClick>로그인</button>
    </div>
}

export default Login;